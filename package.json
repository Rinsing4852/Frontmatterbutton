{
	"name": "frontmatter-button",
	"version": "1.0.1",
	"description": "Add configurable default YAML frontmatter to the current Obsidian note.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"frontmatter",
		"yaml"
	],
	"author": "Ryan Olley",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.30",
		"esbuild": "^0.25.0",
		"obsidian": "^1.8.7",
		"typescript": "^5.4.5"
	}
}
